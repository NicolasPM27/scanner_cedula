# ══════════════════════════════════════════════════════════
# FOMAG — Variables de Entorno
# Copiar como .env y ajustar valores según el ambiente
# ══════════════════════════════════════════════════════════

# ── Ambiente ─────────────────────────────────────────────
NODE_ENV=development          # development | production | staging
PORT=3000
LOG_LEVEL=debug               # debug | info | warn | error

# ── Base de Datos (Azure SQL Edge / Azure SQL Database) ──
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=
DB_NAME=fomag_poblacion
DB_ENCRYPT=false              # true para Azure SQL Database
DB_TRUST_SERVER_CERTIFICATE=true  # false para Azure SQL Database

# Azure SQL Database (producción):
# DB_HOST=fomag-server.database.windows.net
# DB_ENCRYPT=true
# DB_TRUST_SERVER_CERTIFICATE=false

# ── Reintentos de conexión DB ────────────────────────────
DB_CONNECT_RETRIES=5
DB_CONNECT_RETRY_DELAY=3000

# ── CORS (producción: lista de orígenes separados por coma)
# En desarrollo se ignora (todos los orígenes permitidos)
CORS_ORIGIN=http://localhost:4200,http://localhost:8100,capacitor://localhost,http://localhost

# Azure producción (ejemplo):
# CORS_ORIGIN=https://fomag-app.azurewebsites.net,https://fomag-app.azurestaticapps.net,capacitor://localhost,http://localhost

# ── Docker (solo para docker-compose) ───────────────────
SA_PASSWORD=
